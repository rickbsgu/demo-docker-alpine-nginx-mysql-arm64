# Basic Docker Demo App for node alpine image & mysql

Setting up the node container isn't easy on the Mac because of the Arm issues.

This project sets up a viable node environment on ARM alpine image.


